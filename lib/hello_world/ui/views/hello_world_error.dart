import 'package:flutter/material.dart';

class HelloWorldError extends StatelessWidget {
  const HelloWorldError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [Center(child: Text("Deu ruim"))]);
  }
}
